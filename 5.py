"""
Write a simple calculator function calculate(a, b, op) that supports:
+ addition, - subtraction, * multiplication, / division

Use try/except to handle:
- division by zero
- invalid operator (raise a ValueError)
"""

# try running this line first and look a the error (exception) Python outputs
err = 4/0
print(err)